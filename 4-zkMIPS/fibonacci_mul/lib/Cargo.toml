[package]
name = "fibonacci-mul-lib"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
alloy-sol-types.workspace = true
bincode = { version = "1.3.3", optional = true }
zkm-sdk = { workspace = true, optional = true }
rkyv = { version = "0.7.45", features = ["std", "validation"] }

[features]
default = []
host = ["dep:zkm-sdk", "dep:bincode"]