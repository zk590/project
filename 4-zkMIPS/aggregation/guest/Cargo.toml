[package]
name = "aggregation"
version = "1.1.0"
edition = "2021"
publish = false

[dependencies]
sha2 = { git = "https://github.com/ziren-patches/RustCrypto-hashes", package = "sha2", branch = "patch-sha2-0.10.8", features = ["oid"] }
zkm-zkvm = { workspace = true, features = ["verify"] }