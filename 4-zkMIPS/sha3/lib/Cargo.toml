[package]
name = "sha3-lib"
version = "0.1.0"
edition = "2024"

[dependencies]
alloy-sol-types = { workspace = true }
zkm-sdk = { workspace = true, optional = true }
bincode = { version = "1.3.3", optional = true }
rkyv = { version = "0.7.45", features = ["bytecheck", "std"] }

[features]
default = []
host = ["dep:zkm-sdk", "dep:bincode"]